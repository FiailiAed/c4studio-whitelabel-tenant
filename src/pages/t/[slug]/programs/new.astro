---
import TenantLayout from "@layouts/TenantLayout.astro";

const tenant = Astro.locals.tenant;
const membership = Astro.locals.membership;
const isImpersonating = Astro.locals.isImpersonating ?? false;

if (!tenant) return Astro.redirect("/");

const isAdmin = isImpersonating || membership?.tenantRole === "admin";
const canManagePrograms = isAdmin || membership?.tenantRole === "coach";
if (!canManagePrograms) return Astro.redirect(`/t/${tenant.slug}`);
---

<TenantLayout
  title="New Program"
  tenant={tenant}
  currentTab="regs"
  tenantRole={membership?.tenantRole}
  isImpersonating={isImpersonating}
  breadcrumbs={[
    { name: "Regs", href: `/t/${tenant.slug}/regs` },
    { name: "New Program", href: "#" },
  ]}
>
  <div class="max-w-lg mx-auto">
    <div class="mb-6">
      <h1 class="text-xl font-bold text-gray-900">New Program</h1>
      <p class="text-sm text-gray-500 mt-1">Create a registration program for your organization.</p>
    </div>

    <div id="error-banner" class="hidden mb-4 rounded-lg bg-red-50 border border-red-200 px-4 py-3">
      <p class="text-sm text-red-700" id="error-text"></p>
    </div>

    <form id="program-form" class="bg-white rounded-xl border border-gray-200 shadow-sm divide-y divide-gray-100">

      <div class="px-4 py-4 space-y-4">
        <!-- Name -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Program Name <span class="text-red-500">*</span></label>
          <input
            id="name"
            name="name"
            type="text"
            required
            placeholder="e.g. Spring 2026 Boys U12"
            class="block w-full rounded-md border-0 bg-white px-3 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 text-sm"
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block text-sm font-medium text-gray-700 mb-1">Description</label>
          <textarea
            id="description"
            name="description"
            rows="3"
            placeholder="Optional details about this program..."
            class="block w-full rounded-md border-0 bg-white px-3 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 text-sm resize-none"
          ></textarea>
        </div>

        <!-- Price + Capacity row -->
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="price" class="block text-sm font-medium text-gray-700 mb-1">Registration Fee <span class="text-red-500">*</span></label>
            <div class="relative">
              <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">$</span>
              <input
                id="price"
                name="price"
                type="number"
                required
                min="0"
                step="0.01"
                placeholder="125.00"
                class="block w-full rounded-md border-0 bg-white pl-7 pr-3 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 text-sm"
              />
            </div>
          </div>
          <div>
            <label for="capacity" class="block text-sm font-medium text-gray-700 mb-1">Capacity</label>
            <input
              id="capacity"
              name="capacity"
              type="number"
              min="1"
              placeholder="Unlimited"
              class="block w-full rounded-md border-0 bg-white px-3 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 text-sm"
            />
          </div>
        </div>

        <!-- Status -->
        <div>
          <label for="status" class="block text-sm font-medium text-gray-700 mb-1">Status <span class="text-red-500">*</span></label>
          <select
            id="status"
            name="status"
            required
            class="block w-full rounded-md border-0 bg-white px-3 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-inset focus:ring-indigo-600 text-sm"
          >
            <option value="DRAFT">Draft — not visible to parents</option>
            <option value="ACTIVE">Active — open for registration</option>
            <option value="CLOSED">Closed — registration ended</option>
          </select>
        </div>
      </div>

      <!-- Actions -->
      <div class="px-4 py-4 flex items-center justify-between gap-3">
        <a
          href={`/t/${tenant.slug}/regs`}
          class="text-sm font-medium text-gray-600 hover:text-gray-800 transition-colors"
        >
          Cancel
        </a>
        <button
          id="submit-btn"
          type="submit"
          class="relative inline-flex items-center justify-center rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 transition-all disabled:opacity-70 disabled:cursor-not-allowed"
        >
          <span id="submit-text">Create Program</span>
          <svg
            id="submit-spinner"
            class="hidden absolute right-3 size-4 animate-spin text-white"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
          >
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
        </button>
      </div>
    </form>
  </div>
</TenantLayout>

<script define:vars={{ tenantSlug: tenant.slug }}>
  const form = document.getElementById("program-form");
  const submitBtn = document.getElementById("submit-btn");
  const submitText = document.getElementById("submit-text");
  const submitSpinner = document.getElementById("submit-spinner");
  const errorBanner = document.getElementById("error-banner");
  const errorText = document.getElementById("error-text");

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();
    submitBtn.disabled = true;
    submitText.textContent = "Creating…";
    submitSpinner.classList.remove("hidden");
    errorBanner.classList.add("hidden");

    const data = new FormData(form);
    const price = parseFloat(data.get("price"));
    const capacity = data.get("capacity") ? parseInt(data.get("capacity"), 10) : undefined;

    const body = {
      name: data.get("name"),
      description: data.get("description") || undefined,
      priceInCents: Math.round(price * 100),
      capacity,
      status: data.get("status"),
      tenantSlug,
    };

    try {
      const res = await fetch("/api/t/programs/create", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(body),
      });
      const json = await res.json();
      if (!res.ok) throw new Error(json.error ?? "Something went wrong.");
      window.location.href = `/t/${tenantSlug}/regs`;
    } catch (err) {
      errorText.textContent = err.message;
      errorBanner.classList.remove("hidden");
      submitBtn.disabled = false;
      submitText.textContent = "Create Program";
      submitSpinner.classList.add("hidden");
    }
  });
</script>
